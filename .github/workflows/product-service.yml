name: product-service CI/CD

on:
  push:
    paths:
      - 'product-service/**'
      - 'helm/ecommerce/charts/product-service/**'
      - '.github/workflows/product-service.yml'
  pull_request:
    paths:
      - 'product-service/**'
      - 'helm/ecommerce/charts/product-service/**'

env:
  HELM_VERSION: 3.12.0
  KIND_VERSION: v0.20.0

jobs:
  ci-kind:
    runs-on: ubuntu-latest
    name: Test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: ${{ env.HELM_VERSION }}

      - name: Set up Kind
        uses: engineerd/setup-kind@v0.6.2
        with:
          version: ${{ env.KIND_VERSION }}

      # Crear cluster Kind
      - name: Create Kind cluster
        run: |
          kind create cluster \
            --name product-service-ci \
            --wait 60s

      # Validar que el cluster está corriendo
      - name: Verify cluster connection
        run: |
          echo "=== Cluster Info ==="
          kubectl cluster-info
          echo ""
          echo "=== Nodes ==="
          kubectl get nodes
          echo ""
          echo "=== Namespaces ==="
          kubectl get namespaces

      # Lintear chart de product-service
      - name: Helm lint product-service chart
        run: |
          helm lint ./helm/ecommerce/charts/product-service

      # Renderizar template
      - name: Render Helm template
        run: |
          helm template product-service ./helm/ecommerce/charts/product-service \
            -f ./helm/ecommerce/charts/product-service/values.yaml \
            --set global.targetEnvironment=dev \
            > helm-rendered.yaml

      # Subir artefacto
      - name: Upload helm template
        uses: actions/upload-artifact@v4
        with:
          name: product-service-helm-template
          path: helm-rendered.yaml

      # Crear namespace y desplegar
      - name: Create namespace
        run: |
          kubectl create namespace product-service || true

      - name: Helm deploy product-service
        run: |
          helm upgrade --install product-service ./helm/ecommerce/charts/product-service \
            -n product-service \
            -f ./helm/ecommerce/charts/product-service/values.yaml \
            --set global.targetEnvironment=dev \
            --wait \
            --timeout 5m

      # Esperar a que los pods estén listos
      - name: Wait for pods to be ready
        run: |
          kubectl wait --for=condition=Ready pod \
            -l app=product-service \
            -n product-service \
            --timeout=300s

      # Verificar estado del deployment
      - name: Verify deployment status
        run: |
          echo "=== Pods in product-service namespace ==="
          kubectl get pods -n product-service
          echo ""
          echo "=== Services in product-service namespace ==="
          kubectl get svc -n product-service
          echo ""
          echo "=== Deployment Details ==="
          kubectl describe deployment product-service -n product-service

      # Verificar logs en caso de error
      - name: Check logs if deployment failed
        if: failure()
        run: |
          echo "=== Product service pod Logs ==="
          kubectl logs -n product-service -l app=product-service --tail=100 || true
          echo ""
          echo "=== Cluster Events ==="
          kubectl get events -n product-service --sort-by='.lastTimestamp' || true

      # Cleanup
      - name: Delete Kind cluster
        if: always()
        run: |
          kind delete cluster --name product-service-ci
name: payment-service CI/CD

on:
  push:
    paths:
      - 'payment-service/**'
      - 'helm/ecommerce/charts/payment-service/**'
      - '.github/workflows/payment-service.yml'
  pull_request:
    paths:
      - 'payment-service/**'
      - 'helm/ecommerce/charts/payment-service/**'

env:
  HELM_VERSION: 3.12.0
  KIND_VERSION: v0.20.0

jobs:
  ci-kind:
    runs-on: ubuntu-latest
    name: Test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: ${{ env.HELM_VERSION }}

      - name: Set up Kind
        uses: engineerd/setup-kind@v0.6.2
        with:
          version: ${{ env.KIND_VERSION }}

      # Crear cluster Kind
      - name: Create Kind cluster
        run: |
          kind create cluster \
            --name payment-service-ci \
            --wait 60s

      # Validar que el cluster está corriendo
      - name: Verify cluster connection
        run: |
          echo "=== Cluster Info ==="
          kubectl cluster-info
          echo ""
          echo "=== Nodes ==="
          kubectl get nodes
          echo ""
          echo "=== Namespaces ==="
          kubectl get namespaces

      # Lintear chart de payment-service
      - name: Helm lint payment-service chart
        run: |
          helm lint ./helm/ecommerce/charts/payment-service

      # Renderizar template
      - name: Render Helm template
        run: |
          helm template payment-service ./helm/ecommerce/charts/payment-service \
            -f ./helm/ecommerce/charts/payment-service/values.yaml \
            --set global.targetEnvironment=dev \
            > helm-rendered.yaml

      # Subir artefacto
      - name: Upload helm template
        uses: actions/upload-artifact@v4
        with:
          name: payment-service-helm-template
          path: helm-rendered.yaml

      # Crear namespace y desplegar
      - name: Create namespace
        run: |
          kubectl create namespace payment-service || true

      - name: Helm deploy payment-service
        run: |
          helm upgrade --install payment-service ./helm/ecommerce/charts/payment-service \
            -n payment-service \
            -f ./helm/ecommerce/charts/payment-service/values.yaml \
            --set global.targetEnvironment=dev \
            --wait \
            --timeout 5m

      # Esperar a que los pods estén listos
      - name: Wait for pods to be ready
        run: |
          kubectl wait --for=condition=Ready pod \
            -l app=payment-service \
            -n payment-service \
            --timeout=300s

      # Verificar estado del deployment
      - name: Verify deployment status
        run: |
          echo "=== Pods in payment-service namespace ==="
          kubectl get pods -n payment-service
          echo ""
          echo "=== Services in payment-service namespace ==="
          kubectl get svc -n payment-service
          echo ""
          echo "=== Deployment Details ==="
          kubectl describe deployment payment-service -n payment-service

      # Verificar logs en caso de error
      - name: Check logs if deployment failed
        if: failure()
        run: |
          echo "=== Payment service pod Logs ==="
          kubectl logs -n payment-service -l app=payment-service --tail=100 || true
          echo ""
          echo "=== Cluster Events ==="
          kubectl get events -n payment-service --sort-by='.lastTimestamp' || true

      # Cleanup
      - name: Delete Kind cluster
        if: always()
        run: |
          kind delete cluster --name payment-service-ci
name: User service CI/CD

on:
  push:
    paths:
      - 'user-service/**'
      - 'helm/ecommerce/charts/user-service/**'
      - '.github/workflows/user-service.yml'
  pull_request:
    paths:
      - 'user-service/**'
      - 'helm/ecommerce/charts/user-service/**'

env:
  HELM_VERSION: 3.12.0
  KIND_VERSION: v0.20.0

jobs:
  ci-kind:
    runs-on: ubuntu-latest
    name: Test

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: ${{ env.HELM_VERSION }}

      - name: Set up Kind
        uses: engineerd/setup-kind@v0.6.2
        with:
          version: ${{ env.KIND_VERSION }}

      # Crear cluster Kind
      - name: Create Kind cluster
        run: |
          kind create cluster \
            --name user-service-ci \
            --wait 60s

      # Validar que el cluster está corriendo
      - name: Verify cluster connection
        run: |
          echo "=== Cluster Info ==="
          kubectl cluster-info
          echo ""
          echo "=== Nodes ==="
          kubectl get nodes
          echo ""
          echo "=== Namespaces ==="
          kubectl get namespaces

      # Lintear chart de user-service
      - name: Helm lint user-service chart
        run: |
          helm lint ./helm/ecommerce/charts/user-service

      # Renderizar template
      - name: Render Helm template
        run: |
          helm template user-service ./helm/ecommerce/charts/user-service \
            -f ./helm/ecommerce/charts/user-service/values.yaml \
            --set global.targetEnvironment=dev \
            > helm-rendered.yaml

      # Subir artefacto
      - name: Upload helm template
        uses: actions/upload-artifact@v4
        with:
          name: user-service-helm-template
          path: helm-rendered.yaml

      # Crear namespace y desplegar
      - name: Create namespace
        run: |
          kubectl create namespace user-service || true

      - name: Helm deploy user-service
        run: |
          helm upgrade --install user-service ./helm/ecommerce/charts/user-service \
            -n user-service \
            -f ./helm/ecommerce/charts/user-service/values.yaml \
            --set global.targetEnvironment=dev \
            --wait \
            --timeout 5m

      # Esperar a que los pods estén listos
      - name: Wait for pods to be ready
        run: |
          kubectl wait --for=condition=Ready pod \
            -l app=user-service \
            -n user-service \
            --timeout=300s

      # Verificar estado del deployment
      - name: Verify deployment status
        run: |
          echo "=== Pods in user-service namespace ==="
          kubectl get pods -n user-service
          echo ""
          echo "=== Services in user-service namespace ==="
          kubectl get svc -n user-service
          echo ""
          echo "=== Deployment Details ==="
          kubectl describe deployment user-service -n user-service

      # Verificar logs en caso de error
      - name: Check logs if deployment failed
        if: failure()
        run: |
          echo "=== User Service pod Logs ==="
          kubectl logs -n user-service -l app=user-service --tail=100 || true
          echo ""
          echo "=== Cluster Events ==="
          kubectl get events -n user-service --sort-by='.lastTimestamp' || true

      # Cleanup
      - name: Delete Kind cluster
        if: always()
        run: |
          kind delete cluster --name user-service-ci